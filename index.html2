<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Minha Casa + Bonita – Sistema Social</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body { font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; background: #f4f6f7; margin: 0; padding: 0; }
    .topbar { background: #1a5276; color: #fff; padding: 1rem; text-align: center; }
    .topbar h1 { margin: 0; font-size: 1.4rem; }
    .tabs { display: flex; justify-content: center; gap: 0.5rem; background: #154360; padding: 0.5rem; flex-wrap: wrap; }
    .tab-btn { border: none; padding: 0.5rem 1rem; border-radius: 999px; font-size: 0.9rem; cursor: pointer; background: #1f618d; color: #fff; }
    .tab-btn.atual { background: #f1c40f; color: #154360; font-weight: 600; }
    .container { max-width: 1000px; margin: 1.5rem auto; padding: 1rem; }
    .card { background: #fff; border-radius: 10px; padding: 1rem 1.5rem; box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin-bottom: 1rem; }
    h2 { font-size: 1.2rem; margin-top: 0; color: #1a5276; }
    label { font-size: 0.9rem; font-weight: 600; margin-bottom: 0.25rem; color: #2c3e50; display: block; }
    input[type="text"], input[type="date"], input[type="tel"], textarea, select {
      padding: 0.5rem 0.6rem; border-radius: 6px; border: 1px solid #dfe6e9; font-size: 0.9rem; width: 100%; box-sizing: border-box;
    }
    textarea { min-height: 80px; resize: vertical; }
    .linha { display: flex; gap: 1rem; }
    .linha > div { flex: 1; }
    .opcoes { display: flex; flex-wrap: wrap; gap: 0.8rem; font-size: 0.9rem; margin-top: 0.3rem; }
    .opcao { display: flex; align-items: center; gap: 0.35rem; cursor: pointer; }
    button, .btn-link {
      background: #1a7a4a; color: #fff; border: none; padding: 0.7rem 1.4rem; border-radius: 999px;
      font-size: 0.95rem; font-weight: 600; cursor: pointer; margin-top: 0.5rem; display: inline-block; text-decoration: none; text-align: center;
    }
    button:disabled { opacity: 0.6; cursor: not-allowed; }
    .msg { margin-top: 0.6rem; font-size: 0.9rem; padding: 0.5rem 0.8rem; border-radius: 6px; display: none; }
    .msg.ok { display: block; background: #e8f8f0; color: #145a35; border: 1px solid #2ecc71; }
    .msg.erro { display: block; background: #fdedec; color: #c0392b; border: 1px solid #e74c3c; }

    .painel-numeros { display: flex; flex-wrap: wrap; gap: 1rem; margin-bottom: 1rem; }
    .box-num { flex: 1; min-width: 160px; background: #fdfefe; border-radius: 10px; padding: 0.7rem 1rem; border: 1px solid #d6eaf8; }
    .box-num .titulo { font-size: 0.85rem; color: #566573; margin-bottom: 0.3rem; }
    .box-num .valor { font-size: 1.3rem; font-weight: 700; color: #1a5276; }
    table { width: 100%; border-collapse: collapse; font-size: 0.85rem; margin-top: 0.5rem; }
    th, td { padding: 0.35rem 0.4rem; border-bottom: 1px solid #ecf0f1; }
    th { text-align: left; background: #f4f6f7; color: #34495e; position: sticky; top: 0; z-index: 1; }
    .tabela-container { max-height: 300px; overflow: auto; border-radius: 8px; border: 1px solid #e5e7e9; margin-top: 0.5rem; }
    .btn-mini { font-size: 0.75rem; padding: 0.25rem 0.6rem; border-radius: 999px; }
    .tag { display: inline-block; padding: 0.2rem 0.5rem; border-radius: 999px; font-size: 0.7rem; }
    .tag.sm { background: #d5f5e3; color: #145a35; }
    .tag.nao { background: #fdebd0; color: #af601a; }
    .tag.sim { background: #d4efdf; color: #1d8348; }
    .tag.info { background: #d6eaf8; color: #1b4f72; }

    @media (max-width: 700px) {
      .linha { flex-direction: column; }
      .container { padding: 0.5rem; }
    }
  </style>
</head>
<body>
  <div class="topbar">
    <h1>Minha Casa + Bonita – Sistema Social</h1>
  </div>

  <div class="tabs">
    <button class="tab-btn atual" data-tab="pre">Pré-cadastro</button>
    <button class="tab-btn" data-tab="visita">Visita domiciliar</button>
    <button class="tab-btn" data-tab="dash">Dashboard</button>
  </div>

  <div class="container">

    <!-- PRE-CADASTRO -->
    <div id="tab-pre" class="card">
      <h2>Pré-cadastro</h2>
      <form id="formPre">
        <div class="linha">
          <div>
            <label>Código da Moradia</label>
            <input type="text" name="codigoMoradia" placeholder="Ex: MC-001">
          </div>
          <div>
            <label>Data do contato / cadastro</label>
            <input type="date" name="dataVisita">
          </div>
        </div>

        <label>Nome completo *</label>
        <input type="text" name="nomeCompleto" required>

        <div class="linha">
          <div><label>Data de nascimento</label><input type="date" name="dataNascimento"></div>
          <div><label>CPF</label><input type="text" name="cpf"></div>
          <div><label>RG</label><input type="text" name="rg"></div>
        </div>

        <label>Endereço</label>
        <input type="text" name="endereco">

        <div class="linha">
          <div><label>Telefone</label><input type="tel" name="telefone"></div>
          <div>
            <label>WhatsApp?</label>
            <div class="opcoes">
              <label class="opcao"><input type="radio" name="whatsapp" value="Sim"> <span>Sim</span></label>
              <label class="opcao"><input type="radio" name="whatsapp" value="Não"> <span>Não</span></label>
            </div>
          </div>
        </div>

        <label>Renda familiar de até 3 salários mínimos?</label>
        <div class="opcoes">
          <label class="opcao"><input type="radio" name="rendaAte3SM" value="Sim"> <span>Sim</span></label>
          <label class="opcao"><input type="radio" name="rendaAte3SM" value="Não"> <span>Não</span></label>
        </div>

        <label>Reside no município há pelo menos 3 anos?</label>
        <div class="opcoes">
          <label class="opcao"><input type="radio" name="moradia3Anos" value="Sim"> <span>Sim</span></label>
          <label class="opcao"><input type="radio" name="moradia3Anos" value="Não"> <span>Não</span></label>
        </div>

        <label>Proprietário do imóvel?</label>
        <div class="opcoes">
          <label class="opcao"><input type="radio" name="proprietarioImovel" value="Sim"> <span>Sim</span></label>
          <label class="opcao"><input type="radio" name="proprietarioImovel" value="Não"> <span>Não</span></label>
        </div>

        <label>Atualmente reside no imóvel?</label>
        <div class="opcoes">
          <label class="opcao"><input type="radio" name="resideNoImovel" value="Sim"> <span>Sim</span></label>
          <label class="opcao"><input type="radio" name="resideNoImovel" value="Não"> <span>Não</span></label>
        </div>

        <label>Possui outro imóvel?</label>
        <div class="opcoes">
          <label class="opcao"><input type="radio" name="possuiOutroImovel" value="Sim"> <span>Sim</span></label>
          <label class="opcao"><input type="radio" name="possuiOutroImovel" value="Não"> <span>Não</span></label>
        </div>

        <label>Observações</label>
        <textarea name="observacoes"></textarea>

        <button type="submit" id="btnPre">Salvar pré-cadastro</button>
        <div id="msgPre" class="msg"></div>
      </form>
    </div>

    <!-- VISITA -->
    <div id="tab-visita" class="card" style="display:none;">
      <h2>Visita domiciliar</h2>
      <p>Para registrar a visita domiciliar detalhada, utilize o formulário específico:</p>
      <a href="visita.html" class="btn-link" target="_blank">Abrir formulário de visita</a>
    </div>

    <!-- DASHBOARD -->
    <div id="tab-dash" class="card" style="display:none;">
      <h2>Dashboard em tempo real</h2>
      <div class="painel-numeros">
        <div class="box-num">
          <div class="titulo">Pré-cadastros registrados</div>
          <div class="valor" id="numPre">-</div>
        </div>
        <div class="box-num">
          <div class="titulo">Visitas realizadas</div>
          <div class="valor" id="numVis">-</div>
        </div>
      </div>

      <h3>Pré-cadastros</h3>
      <div class="tabela-container">
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Data</th>
              <th>Família</th>
              <th>Cód. Moradia</th>
              <th>Renda ≤ 3 SM</th>
              <th>3 anos cidade</th>
              <th>PDF</th>
            </tr>
          </thead>
          <tbody id="tPre"></tbody>
        </table>
      </div>

      <h3 style="margin-top:1.2rem;">Visitas domiciliares</h3>
      <div class="tabela-container">
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Data</th>
              <th>Família</th>
              <th>Cód. Moradia</th>
              <th>Renda ≤ 3 SM</th>
              <th>Faixa de renda</th>
              <th>Tipo moradia</th>
              <th>PDF</th>
            </tr>
          </thead>
          <tbody id="tVis"></tbody>
        </table>
      </div>

      <div id="msgDash" class="msg" style="margin-top:0.8rem;"></div>
    </div>

  </div>

<script>
document.addEventListener('DOMContentLoaded', function() {

  var SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbzx7rrwybDqsdZ8lFTVpVsy0gm3uvFSzf_qXlzL4Y0KDuEjM6kRukYnnvxnZZDR_aef/exec';

  // Tabs
  document.querySelectorAll('.tab-btn').forEach(function(btn) {
    btn.addEventListener('click', function() {
      document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('atual'));
      this.classList.add('atual');

      var tab = this.getAttribute('data-tab');
      document.getElementById('tab-pre').style.display   = (tab === 'pre'   ? 'block' : 'none');
      document.getElementById('tab-visita').style.display= (tab === 'visita'? 'block' : 'none');
      document.getElementById('tab-dash').style.display  = (tab === 'dash'  ? 'block' : 'none');

      if (tab === 'dash') carregarDashboard();
    });
  });

  // Pré-cadastro
  document.getElementById('formPre').addEventListener('submit', function(e) {
    e.preventDefault();

    var msg = document.getElementById('msgPre');
    var btn = document.getElementById('btnPre');
    msg.textContent = ''; msg.className = 'msg'; 
    btn.disabled = true; btn.textContent = 'Enviando...';

    var fd    = new FormData(document.getElementById('formPre'));
    var dados = {};
    fd.forEach(function(v, k) { dados[k] = v; });

    dados.emailTecnico = 'plantao@exemplo.com';
    dados.nomeTecnico  = 'Plantão Social';

    var params = new URLSearchParams();
    params.append('action',  'salvarPreCadastro');
    params.append('payload', JSON.stringify(dados));

    fetch(SCRIPT_URL, { method: 'POST', body: params })
      .then(function(r) { return r.json(); })
      .then(function(json) {
        if (json.sucesso) {
          msg.className   = 'msg ok';
          msg.textContent = 'Pré-cadastro salvo! ID: ' + json.id;
          document.getElementById('formPre').reset();
        } else {
          msg.className   = 'msg erro';
          msg.textContent = 'Erro: ' + (json.mensagem || 'não foi possível salvar.');
        }
      })
      .catch(function() {
        msg.className   = 'msg erro';
        msg.textContent = 'Erro de comunicação com o servidor.';
      })
      .finally(function() {
        btn.disabled = false;
        btn.textContent = 'Salvar pré-cadastro';
      });
  });

  // Dashboard
  function carregarDashboard() {
    var msg = document.getElementById('msgDash');
    msg.textContent = ''; msg.className = 'msg';

    var params = new URLSearchParams();
    params.append('action', 'getDashboardData');

    fetch(SCRIPT_URL, { method: 'POST', body: params })
      .then(function(r) { return r.json(); })
      .then(function(json) {
        if (!json.sucesso) {
          msg.className   = 'msg erro';
          msg.textContent = 'Erro ao carregar dashboard: ' + (json.mensagem || '');
          return;
        }

        document.getElementById('numPre').textContent = json.preCadastros.total;
        document.getElementById('numVis').textContent = json.visitas.total;

        // Pre-cadastros
        var tPre = document.getElementById('tPre');
        tPre.innerHTML = '';
        json.preCadastros.registros.forEach(function(r) {
          var tr = document.createElement('tr');
          tr.innerHTML =
            '<td>' + r.id + '</td>' +
            '<td>' + (r.dataHora || '') + '</td>' +
            '<td>' + (r.nomeCompleto || '') + '</td>' +
            '<td>' + (r.codigoMoradia || '') + '</td>' +
            '<td>' + tagSimNao(r.rendaAte3SM) + '</td>' +
            '<td>' + tagSimNao(r.moradia3Anos) + '</td>' +
            '<td><button class="btn-mini" data-pdf-pre="' + r.id + '">PDF</button></td>';
          tPre.appendChild(tr);
        });

        // Visitas
        var tVis = document.getElementById('tVis');
        tVis.innerHTML = '';
        json.visitas.registros.forEach(function(r) {
          var tr = document.createElement('tr');
          tr.innerHTML =
            '<td>' + r.id + '</td>' +
            '<td>' + (r.dataHora || '') + '</td>' +
            '<td>' + (r.nomeCompleto || '') + '</td>' +
            '<td>' + (r.codigoMoradia || '') + '</td>' +
            '<td>' + tagSimNao(r.rendaAte3SM) + '</td>' +
            '<td>' + (r.faixaRenda ? '<span class="tag info">' + r.faixaRenda + '</span>' : '') + '</td>' +
            '<td>' + (r.tipoMoradia || '') + '</td>' +
            '<td><button class="btn-mini" data-pdf-vis="' + r.id + '">PDF</button></td>';
          tVis.appendChild(tr);
        });

        msg.className   = 'msg ok';
        msg.textContent = 'Dashboard atualizado.';

        // Eventos de PDF
        document.querySelectorAll('[data-pdf-pre]').forEach(function(btn) {
          btn.addEventListener('click', function() {
            gerarPdfPre(this.getAttribute('data-pdf-pre'));
          });
        });
        document.querySelectorAll('[data-pdf-vis]').forEach(function(btn) {
          btn.addEventListener('click', function() {
            gerarPdfVis(this.getAttribute('data-pdf-vis'));
          });
        });

      })
      .catch(function() {
        msg.className   = 'msg erro';
        msg.textContent = 'Erro de comunicação com o servidor.';
      });
  }

  function tagSimNao(v) {
    if (!v) return '';
    if (v.toLowerCase() === 'sim') return '<span class="tag sim">Sim</span>';
    if (v.toLowerCase().startsWith('não') || v.toLowerCase().startsWith('nao')) return '<span class="tag nao">Não</span>';
    return '<span class="tag">' + v + '</span>';
  }

  function gerarPdfPre(id) {
    var params = new URLSearchParams();
    params.append('action', 'gerarPdfPreCadastro');
    params.append('id', id);

    fetch(SCRIPT_URL, { method: 'POST', body: params })
      .then(function(r) { return r.json(); })
      .then(function(json) {
        if (json.sucesso && json.urlPdf) {
          window.open(json.urlPdf, '_blank');
        } else {
          alert('Erro ao gerar PDF: ' + (json.mensagem || ''));
        }
      })
      .catch(function() {
        alert('Erro de comunicação ao gerar PDF.');
      });
  }

  function gerarPdfVis(id) {
    var params = new URLSearchParams();
    params.append('action', 'gerarPdfVisita');
    params.append('id', id);

    fetch(SCRIPT_URL, { method: 'POST', body: params })
      .then(function(r) { return r.json(); })
      .then(function(json) {
        if (json.sucesso && json.urlPdf) {
          window.open(json.urlPdf, '_blank');
        } else {
          alert('Erro ao gerar PDF: ' + (json.mensagem || ''));
        }
      })
      .catch(function() {
        alert('Erro de comunicação ao gerar PDF.');
      });
  }

});
</script>
</body>
</html>
